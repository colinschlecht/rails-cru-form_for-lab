<h1>Edit Artist Form: </h1>
<%= form_for @artist do |f| %>
<label>Artist Name:</label>
<%= f.text_field :name %> <br>
<label>Artist Bio</label> <br>
<%= f.text_area :bio %>  <br>
<%= f.submit %>
<% end %>
<%= link_to 'cancel edit', artist_path(@artist) %>